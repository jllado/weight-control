interface:
  display_name: "Release Plan"
  short_description: "Implement and release approved plans"
  default_prompt: "Use $release-plan to implement the approved plan and release it to production."
policy:
  allow_implicit_invocation: false
